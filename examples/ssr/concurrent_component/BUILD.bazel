load("@npm//@bazel/typescript:index.bzl", "ts_library")
load("//:index.bzl", "prerender_component")

prerender_component(
    name = "concurrent_component",
    srcs = ["concurrent_prerender.ts"],
    ssr = [":concurrent_ssr"],
    lib_deps = ["//packages/rules_prerender"],
    visibility = ["//examples/ssr:__subpackages__"],
)

ts_library(
    name = "concurrent_ssr",
    srcs = ["concurrent_ssr.ts"],
    deps = [
        "//common/models:json",
        "//packages/ssr",
    ],
)
